# 群聊与管理者调度分析

**日期**：2026-09-17  
**性质**：产品与运行时分析。除「会话点击」已落地外，群时间线、能力池物化、异步回告均未实现。  
**范围**：工作台「用户 → 管理者 → 工人」这一层。不改各家官方回路本身。

---

## 1. 这份文档从哪来

连续两轮讨论：

1. 实现上已经互相卡住（死胡同）。
2. 按「会话 = 群聊房间」重画交互，并客观回答五个问题。

随后拍板了**会话点击的第一步**，其余未拍板项仍按建议写在文末。

用户当时的五个问题（原文收口）：

1. 点会话进入类似群聊；右侧是用户发言；管理者接收后调用项目能力分发任务。
2. 群聊里管理者和被派的其他 AI 都在；可以一句对一句，也可以是总结。
3. 点会话仍能看到这场所有 AI；单独点某个 AI 看执行过程。执行间左侧是正常 Agent 流程，右侧是管理者派的任务 + 用户直接派的任务。
4. 多一步管理者是否合理？管理者怎么调用各个 AI？各个 AI 完成后怎么通知管理者？
5. 会话点击具体怎么做？

---

## 2. 总判断

**多一步管理者是合理的**，前提只有一条：它只做「接待 + 拆任务 + 回收结果」，不代替工人干活。

产品形状：

```text
会话 = 群聊房间
管理者 = 固定角色（演员可换）
其他 AI = 被派出去的工人
```

这一刀能解开「分区谁来建」：工人不该由用户在会话里加号创建，而该在管理者（或用户点某个 AI）第一次派发时出现。

卡住的不是「该不该有管理者」，而是两件事：

- **群聊时间线**和**工人执行过程**必须是两份数据。
- **回告由运行时做**，禁止模型互叫。

---

## 3. 先解开的四条死胡同

不是再改一版 UI 就能绕开。根因是「全局一份管理者」和「会话一份管理者分区」对不齐。

| # | 死胡同 | 现状 | 为何卡死 |
|---|---|---|---|
| 1 | 管理者到底是全局一个，还是每个会话一个 | 设置/页脚切的是全局 `state.manager`；树里每个会话又挂着「管理者AI」分区 | `POST /api/manager` 规定不改会话，所以「当前用谁」写不进分区。切完后：正在生成的那一轮冻住，同一会话下一句和其他会话立刻换模型 |
| 2 | 分区不能手建，调度却还在 | 「＋ 添加 AI 分区」已去掉；管理者仍带 `workbench_list_partitions` / `workbench_dispatch` | 没有工人入口。旧数据会残留，`attachModel` 还在，用户加不了。树第三层几乎永远只有「管理者AI」 |
| 3 | 历史绑在角色上，不绑在当次模型上 | 对话记在固定的 `manager` transcript | Claude 切到 GPT，下一轮 GPT 读的是 Claude 的上下文。`freezeManager` 只保护正在生成的那一轮 |
| 4 | 树上永远写着「管理者AI」 | 模型只在页脚/设置里变 | 切了看不出换了谁，和工作区体感对不上 |

第 1 条是根。不定「角色 vs 演员」，2、3、4 怎么改都会再撞墙。

群聊方案给出的切法（建议，尚未全部落地）：

- **角色固定**：会话永远有一个管理者入口，不必在树里再挂一个可点的「管理者AI」私聊。
- **演员全局可换**：设置/页脚选谁来演管理者；开跑后这一轮用快照，不跟中途切换走。
- **工人另开一条路**：来自已配置能力池，第一次 dispatch 时才挂到该会话树下。不要再靠用户点「添加分区」。

比较干净的另外两种切法（若推翻群聊方案才回头用）：

- **全局管理者**：会话里不再挂管理者分区；点会话就跟当前管理者聊。
- **会话管理者**：每个会话自己记用谁；设置/页脚只改当前会话（或只当新建默认值）。

---

## 4. 两套界面必须拆开

第 1、2 点说的，和第 3 点说的，不是同一块屏幕。混在一起，左右栏会对不上。

| | 点会话（群聊） | 点某个工人 AI（执行间） |
|---|---|---|
| 这是什么 | 整场任务的对外时间线 | 某一个工人的完整 Agent 过程 |
| 谁在说话 | 用户、管理者、工人的**短发言 / 摘要 / 任务卡片** | 只有这个 AI 的思考、工具、HITL |
| 输入框发给谁 | 默认只给管理者 | 等于用户直接给这个 AI 下任务 |
| 左右 | 群聊气泡（用户在右、别人在左） | 左：正常回答流程；右：**任务箱**（管理者派的 + 用户直接派的） |

现在实现：点会话会自动选中「管理者AI」，中间只有一份 transcript。那是「私聊某一个分区」，**还不是群聊**。按设想，点会话不该再掉进管理者的私聊。

「用户在右侧」只属于群聊。不要和执行间右侧的「任务箱」做成同一块——那会把发言和任务单混在一起。

---

## 5. 按五个问题

### 5.1 点会话进群聊 —— 成立，但默认只跟管理者说话

用户进会话只面对一个入口，由管理者决定要不要动项目能力、要不要派人。否则用户每次都要先想「该点 Claude 还是 Codex」。

建议写死成三条：

1. 群聊里用户发的每一句，**只进管理者**，不要广播给所有 AI。
2. 管理者可以自己答（闲聊、问一句、已经能做的事），**不要每句都分发**。否则多一层只有延迟和钱。
3. 只有需要别家工具面、需要并行、或管理者明确决定「这活不该我干」时，才调用项目能力去分发。

用户要直接下令：去点那个工人 AI，不要在群聊里 @ 全员。

### 5.2 一句对一句，也可以是总结 —— 两种都要，但是不同消息类型

群聊如果把每个工人的工具轨迹都摊开，会不可读。群聊只允许三种东西：

| 类型 | 做什么 | 不做什么 |
|---|---|---|
| **发言** | 管理者说「我让 Claude 去改这一处」；工人回来后一句结果或失败原因 | 不贴思考过程和工具轨迹 |
| **总结** | 工人都回来之后，管理者对用户说人话 | 不把各家 transcript 拼在一起 |
| **任务卡片** | 可折叠，「Claude · 进行中 / 完成」；点进去才是执行间 | 卡片不是聊天记录 |

- 「一句对一句」适合：管理者交代 → 工人回一句 → 管理者再总结。
- 「总结」适合：并行多个工人时，群聊里先出现卡片，最后管理者说一段。

不要做成真·全员群：用户一句话所有模型一起抢答——贵、乱、也没法当唯一事实来源。

工人的思考、工具、权限询问留在执行间。群聊是给人看的，执行间是给排查和接管用的。

### 5.3 点会话看全员，点 AI 看执行 —— 主路径和钻取

和现在的树（项目 → 会话 → AI）对齐，但点击含义要改：

- **点会话**：打开群聊（整场）。树可以展开，让你看见这场里有哪些 AI，主区仍是群聊。
- **点某个工人 AI**：打开执行间。左是它自己的 Agent 流程；右是它的任务箱。
- **点「管理者AI」**：建议和点会话是**同一件事**（仍是群聊）。不要再给管理者做一个第三种「私聊」。管理者和用户的对话就是群聊本身。

执行间右侧任务箱是有必要的：这个 AI 现在在干嘛、是谁让它干的。用户在这里发消息 = 直接给这个工人加一条任务。

用户直接给工人下任务时，**要告诉管理者**。建议在群聊里插一条系统事实：「用户直接向 Claude 下达了 …」。不必为此再开一轮完整对话，记一条事件就够，必要时再让管理者补一句。否则管理者后面总结时，世界是过时的。

从执行间回群聊：再点一次会话名，或顶上一粒「返回会话」。不要靠浏览器后退。

### 5.4 多一步管理者合理吗？怎么调用、怎么回告？

**合理**。各家官方回路的工具面本来就不一样（Claude / Codex / Grok / Gemini），工作台又不能做成统一 ToolRegistry。需要一个只属于工作台的角色去选人、汇总，并且**禁止工人去冒充别家工具**。

现在 overlay 的方向（`workbench_list_partitions` / `workbench_dispatch`）就是这个角色，只是还停在「同步工具调用」，不是群聊。

不合理、应写成硬约束的三种情况：

- 这场里实际只能用一个模型：管理者自己答，不要假分发。
- 管理者把工人的活又重做一遍（读完结果再改一遍代码）。
- 工人再「派回」管理者，形成模型之间互相喊。回告必须是运行时协议，不是 LLM 再聊一轮。

#### 工人从哪来（死胡同的根，原问题没写）

用户不在会话里「＋添加分区」是对的。工人应来自**已配置、已登录的能力池**（官方四家 + 有密钥的自定义），而不是会话里手建的空分区。

建议生命周期：

1. 设置里选的是「谁来演管理者」——角色固定叫管理者，演员可换；开跑后这一轮不换。
2. 管理者分发时，按任务选能力池里的人，**这场会话里才出现这个 AI**（第一次派发时才挂到树下）。
3. 树下的工人 = 「这场用过 / 正在用的人」，不是用户预先排的班。

自定义连接没有 overlay、也不能当工人去跑官方工具面。产品上要么管理者只允许官方四家，要么给自定义也补上同一套分发工具（工人仍然跑各自回路）。

#### 调用（管理者 → 工人）

不要让两个模型直接对话。固定成工作台工具：

```text
dispatch({ to, prompt, taskId })
```

- 管理者只决定：派给谁、任务怎么写。
- 真正跑哪套 vendor harness，由工作台根据那个 AI 的连接来选。
- 工人看不到「管理者是谁」，只看到一条任务（user 消息进它自己的 transcript）。
- 一次管理者回合里允许派多个。每派一个，群聊里立刻出现一张「进行中」卡片。
- 工人不能再 dispatch（现在 `depth = 1` 就是这个意思）。否则会套娃。

并行是目标形态；现在代码是一个接一个等。

#### 回告（工人 → 管理者）

**不要**让工人再调一个「通知管理者」的工具。工人跑完，**工作台**通知管理者。两条路径都要，按任务长短用：

**短任务（先做，和现在接近）**

管理者的 `dispatch` 是同步工具：工人整轮跑完，截一段结果当 tool result 回到管理者，管理者再对用户说话。

「通知」= 工具返回值。群聊顺序：用户 → 管理者（派人）→ 工人卡片（完成）→ 管理者（总结）。

**长任务（群聊要「同时说话」才需要）**

`dispatch` 立刻返回 `{ taskId, status: running }`。工人在后台跑。结束时运行时做三件事：

1. 把工人 transcript 收尾（执行间能看完）。
2. 往群聊时间线写一条事件/卡片：谁完成了、摘要、失败的话带原因。
3. 若管理者还在等：把结果填进那次 tool call。若管理者已经说完了：再给管理者塞一条**系统续跑**（「Claude 完成了任务 X：…」），让它决定要不要再对用户说一句。

这就是「通知到管理者」，不是工人 @ 管理者。

#### HITL 与中止

- 权限询问不要经过管理者代批。工人要批文件/命令，出现在该工人的执行间；群聊里只留「等待你确认」的卡片。
- 群聊里停 = 管理者 + 这场所有在跑的分发。
- 执行间里停 = 只停这个工人，并回告管理者「被用户中止」。

进行中途在设置里切换管理者：只影响**下一句用户消息**；当前这一轮管理者和已派出的工人都继续用开跑时的快照。

### 5.5 会话点击怎么做

分析时建议把「展开」和「打开」分开，否则树会和主区抢：

1. 会话行的文字/空白处：打开群聊。
2. 会话行的三角：只展开/收起下面的 AI 名单，不换主区。
3. 展开后的工人：点它打开执行间。
4. 展开后的管理者AI：若仍显示在树里，点击 = 与点会话相同。更好是树里不单独列管理者，名单上只列被派过/正在跑的工人。
5. 新建会话：自动带管理者角色，立刻进群聊（空时间线）。
6. 进行中的标记：会话行和对应 AI 行能看出谁在跑。

**已拍板并落地的第一步**（见第 7 节）：第一次点会话 = 进入该会话（群聊主区）并展开 AI 列表；已在该会话群聊再点同一会话 = 只收起/再展开 AI 列表，不离开会话。三角与文字尚未拆成两个热区。

---

## 6. 和现状差在哪

| 已有 | 没有（产品骨架，不是再加一个按钮） |
|---|---|
| 管理者角色 `{ role:'manager', key:'manager', name:'管理者AI' }` | 群时间线对象（现在按 `manager` / `partition-id` 各存一份 transcript） |
| 开跑快照 `freezeManager` | 分发作为群里的独立发言者 |
| 同步 `workbench_dispatch`（堵在管理者回合里，摘要截 6000 字，depth 1） | 异步回告、并行、系统续跑 |
| 工人自己的 transcript | 执行间右侧任务箱 |
| 禁止派给管理者自己 | 能力池 → 会话成员 的物化 |
| 会话点击：进群聊主区 + 展开/收起 AI 列表 | 点会话不再加载管理者私聊；树下可隐藏管理者AI |

当前数据拓扑（实现层）：

```text
本地项目
  └── 会话 session
        ├── 管理者分区（自动挂上，用户不能删）
        └── 工人分区（现无正规入口；旧数据 / attachModel 残留）
全局 state.manager { connectionId, model, effort?, contextWindow? }
  └── POST /api/manager 不改会话
```

点会话现在仍会 `selectedPartition = 管理者分区`，再 `loadChatHistory(session, '')`，中间仍是管理者私聊，不是群时间线。

---

## 7. 已拍板 / 未拍板

### 已拍板

| 项 | 结论 |
|---|---|
| 会话第一次点击 | 中间进入该会话（群聊/管理者主区），并展开树下 AI 列表 |
| 已在该会话群聊再点同一会话 | 只开关 AI 列表，不离开会话，不重载对话 |
| 从工人执行间再点该会话 | 视为进入（回到群聊并展开列表），不是开关列表 |
| 点列表里的某个 AI | 仍进该 AI 的执行页 |
| 点另一个会话 | 切过去并展开那个会话的 AI 列表 |
| 本步范围 | 只做点击 + 列表展开/收起；不做群时间线、异步分发、能力池物化 |

实现要点（`public/app.js`）：捕获阶段拦住原生 `<details>` 默认展开；已在群聊时只改 `open` 集合和 `details.open`，不做整页重绘。

群聊判定：`selectedSession === 该会话 && page === 'workspace' && 当前不是某个工人`。

第三次点击（列表已收起）会再展开列表——否则收起后没法从会话行打开。这是落地时补的产品收口，原话只覆盖「再点则关闭」。

### 未拍板（不定后面会再分叉）

| # | 问题 | 建议 |
|---|---|---|
| 1 | 群聊输入是否永远只给管理者 | 是。用户要直接下令，去点那个 AI |
| 2 | 树里还要不要单独列出「管理者AI」 | 不要。会话本身就是管理者入口，树下只列工人 |
| 3 | 没有可派的工人时 | 管理者自己干，并在群里说明「当前只有我」；不要空转 dispatch |

---

## 8. 建议落地顺序

仍只是建议。先不要做全员自由群聊，也不要做工人回调管理者的模型对话。

1. **能力池 + 同步分发进群卡片**  
   工人从已配置连接来；第一次 dispatch 才挂到该会话；群聊出现进行中/完成卡片。分发仍可先同步。
2. **点会话 = 群聊 / 点 AI = 执行间**  
   主区两套界面；群输入只进管理者；点管理者 = 点会话（或树里不再列出管理者）。
3. **异步回告和并行**  
   `dispatch` 立即返回 running；结束后系统写群事件，必要时给管理者系统续跑。HITL 仍在该工人执行间。

会话点击的第一步已经做完，停在第 1 步之前。群聊发言、管理者派发、工人回传都还没动。

---

## 9. 硬约束（本文不改这些）

- catalog 仍是模型来源的唯一事实。
- 管理者 AI：同一系列一张卡；官方登录与自定义提供商互斥。
- 设置 Tabs 只有「模型提供商」和「管理者 AI」。
- `POST /api/manager` 不改写会话。
- 不做 cc-switch 代理，不写 `~/.claude` / `~/.codex`。
- 官方 Gemini = Antigravity（`agy`）OAuth，不是 Gemini CLI。
- 不 spawn 本机 `agy` / `claude` / `codex` / `grok`。
- 没有真实 PTY。
- 四套 vendor loop，不是统一 ToolRegistry。
- 不改 `参考/`、`各家Agent的内置技能/`、`测试1*`、`licenses/`、`src-tauri` 构建产物。

更早的拓扑说明见 `各家Agent的内置技能/工作台主AI对多AI.md`。那份只定「用户 → 主 AI → 多分区」的形状；本文是在它上面收口群聊、点击和回告，并以当前代码为准。
